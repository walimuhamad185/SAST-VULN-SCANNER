# Core engine: zero mandatory dependencies (Python 3.8+ stdlib only)

# Optional: local AI re-verification layer (Ollama via OpenAI-compatible API)
openai>=1.0.0

# Optional: YAML config support (sast.yaml)
pyyaml>=5.1

# Optional: PDF report export
# weasyprint>=60.0
